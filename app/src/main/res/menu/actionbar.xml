<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    >
    <item android:id="@+id/action_search" android:title=""
        android:icon="@drawable/ic_search"
        app:showAsAction="always"

        >
        <menu>
            <item
                android:id="@+id/search_by_name"
                android:showAsAction="never"
                android:title="Client/Provider Name" />
            <item
                android:id="@+id/search_by_date"
                android:showAsAction="never"
                android:title="Refil Due Date" />
            <item
                android:id="@+id/search_by_all"
                android:showAsAction="never"
                android:title="All" />
        </menu>

    </item>
    <item android:id="@+id/action_logout" android:title=""
        android:icon="@drawable/ic_logout"
        app:showAsAction="always"

        >
        </item>
</menu>